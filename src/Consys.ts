'use strict';

/**
 * Consys class
 *
 * @class Consys
 */
export default class Consys {
  /**
   * Creates a condition that allows only n simultaneous executions
   * of a guarded function
   * @param {number} number number that gets returned
   * @returns {number} number which was given as input
   */
  static exampleValue(number: number): number {
    return number;
  }
}
